# enable usb_charge everywhere
always, usbcharge, on
# enable cpu overclock in games
game, overclock, off
# enable powersave (cpu underclock) in xmb
vsh, powersave, off
# enable custom menu launcher
always, launcher, off
# disable pause game (only for PSP Go)
always, disablepause, off
# enable high memory for homebrews
homebrew, highmem, on
